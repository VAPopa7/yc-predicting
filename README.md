# yc-predicting
Use historical bond yield data across maturities to extract yield curve factors via PCA, and use those factors to predict recession probability (defined as two consecutive negative GDP quarters) via a binary response model.
